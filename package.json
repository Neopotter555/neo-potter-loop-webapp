{
  "name": "neo-potter-loop-webapp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node scripts/build.js",
    "start": "npx serve@latest public",
    "deploy": "npx netlify-cli deploy --prod --dir=public"
  },
  "devDependencies": {}
}
